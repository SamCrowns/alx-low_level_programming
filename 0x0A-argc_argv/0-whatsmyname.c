#include "main.h"
#include <stdio.h>
/**
 * main - Function that prints its name
 * @argc: argument count
 * @argv: argument vector
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	printf("%d", argv[0]);
	return (0);
}
